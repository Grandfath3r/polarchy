#!/bin/bash

# Finish the installation of Polarchy with items that can only be done after logging in.

set -e

FIRST_RUN_MODE=~/.local/state/polarchy/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$POLARCHY_PATH/install/first-run/battery-monitor.sh"
  bash "$POLARCHY_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$POLARCHY_PATH/install/first-run/firewall.sh"
  bash "$POLARCHY_PATH/install/first-run/dns-resolver.sh"
  bash "$POLARCHY_PATH/install/first-run/gnome-theme.sh"
  bash "$POLARCHY_PATH/install/first-run/elephant.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$POLARCHY_PATH/install/first-run/welcome.sh"
  bash "$POLARCHY_PATH/install/first-run/wifi.sh"
fi
